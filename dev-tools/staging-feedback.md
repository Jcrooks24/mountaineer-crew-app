# Staging feedback briefing

Generated 2026-07-08T15:57:48.247Z from 4 unresolved Vercel Toolbar thread(s).
Source map: `dev-tools/component-map.json` — regenerate with `npm run map:components`.

---

# Mapped to source

## /?tab=timeline — RodsRecorder (`frontend/src/components/RodsRecorder.tsx`)
Thread ID: vc_thread_a1

> The duty-status buttons are too small to tap with gloves on. Can we make them bigger on mobile?

Replies:
- **Dispatch:** Agreed, the drivers keep mis-tapping ON DUTY vs DRIVING.

## / — ReportPerDiem (`frontend/src/components/JobReport.tsx`)
Thread ID: vc_thread_b2

> Per-diem wording should say 'per crew member per day' explicitly so nobody thinks it's a flat $50.

## /long-distance — LdTralaExemption (`frontend/src/pages/LongDistance.tsx`)
Thread ID: vc_thread_c3

> Add a link to download the TRALA exemption PDF so drivers can save it offline.

# Unmapped

_These threads couldn't be matched to a `data-component`. Add the tag to the
 element (see dev-tools README) so they map next time._

## /?tab=report — (no component tag)
Thread ID: vc_thread_d4

> This save button doesn't show a spinner and I tapped it twice by accident.

---

## Threads to resolve after addressing
_Check off + resolve in the Vercel Toolbar (or via the Vercel MCP) once handled._

- [ ] vc_thread_a1 — /?tab=timeline (RodsRecorder)
- [ ] vc_thread_b2 — / (ReportPerDiem)
- [ ] vc_thread_c3 — /long-distance (LdTralaExemption)
- [ ] vc_thread_d4 — /?tab=report
